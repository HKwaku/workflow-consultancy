import '../../public/styles/diagnostic.css';
import '../../public/styles/flow-canvas.css';
import '../../lib/modules/report/report.css';

// Standalone full-page mount for a deal_analysis row, opened via the
// "Open in new tab" link in the inline analysis viewer's top bar.
export default function DealAnalysisLayout({ children }) {
  return children;
}
